# LinkVault Environment Variables
# Copy this file to .env and fill in your values.

# ─── Database ────────────────────────────────────────────
# Local development: uses SQLite file (no config needed)
# Just leave these commented out for local dev.

# Turso (production / remote database)
# Get these from: https://turso.tech → Create database → Connect
DATABASE_URL=libsql://your-database-name.turso.io
DATABASE_AUTH_TOKEN=your-auth-token-here
